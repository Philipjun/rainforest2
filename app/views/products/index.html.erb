<h1>Products Page</h1>

<% @products.each do |product| %>
  <ul>
  <%= product.name %> : <br/>
  <%= product.formatted_price %>
  
  </uL> 

<% end %>

<%= link_to "Add new product", new_product_path %>

